'use strict';

const express = require('express');
const router = express.Router();
// var movie = require('../models/personSchema');
// var winston = require('../components/winston');

router.get('/', (req, res) => {

});

router.post('/', (req, res) => {

});

module.exports = router;
